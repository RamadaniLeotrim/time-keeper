# Turso Database URL (Begins with libsql:// or https://)
VITE_TURSO_DATABASE_URL=libsql://your-db-name-user.turso.io

# Turso Auth Token (Required for remote connection)
# WARNING: In a client-side app, this token is visible to users. 
# Only use for private tools or with read-only tokens where appropriate.
VITE_TURSO_AUTH_TOKEN=your-auth-token
